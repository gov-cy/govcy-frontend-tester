name: Unit test

on:
  push:
    branches:
      - main
    paths:
      - '**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 14
    - name: Install dependencies
      run: npm ci
    - name: Test
      run: npm test
    - name: Upload report to artifact
      uses: actions/upload-artifact@v2
      with:
        name: report
        paths: ["reports/index.html", "reports/reportJSON.txt"
        